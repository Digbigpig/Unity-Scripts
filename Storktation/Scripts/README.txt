Top scripts worth viewing:
    LabelTextureGenerator
        - This dynamically creates a texture of text that says where the package is destined for. 
    Label
        - The related Label object that uses the generator to greate the label
    Package
        - Contains the functions for generating and interacting with packages.
    Sensor
        - This controls the flow of packages by detecting when packages have blocked the sensor.
